

// creating a function for recursion (2x)
<myFunc> = func
    // calling the print function
    <995> = <inc>
    call <print>

    // waiting a second
    <995> = 250
    call <sleep>

    // doubling the value and calling the function
    <inc> = <inc> * 2

    if <inc> < 1000
        if <inc> < 100
            call <myFunc>
            done
        <995> = 50
        call <print>
        done

    // ending the function
    retrn


// setting the initial value and calling the function
<inc> = 1
call <myFunc>

// setting <i> to 0
<i> = 0

// call a print with a value of 99
<995> = 99
call <print>

// waiting a second
<995> = 250
call <sleep>

<i> = <i> + 1  // adding one to it

// jumping back if <i> < 2 (<i> <= 1)
if <i> <= 1
    jmp 35
    done

// ending the program
end


